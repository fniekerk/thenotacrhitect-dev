# The Not Architect — Project Brief

A technical and business/niche content blog (thenotarchitect.dev) built with full code control, best-practice architecture, global security standards, and ≥80% test coverage.

## Branding

| Property | Value |
|---|---|
| Blog name | The Not Architect |
| Domain | thenotarchitect.dev |
| Tagline | *I draw boxes, cross them out, and ship.* |
| Logo | `public/thenotarchitect_logo_final.svg` (light text — render on a dark panel) |
| Palette | burnt orange `#e05a2b` (accent), C4 blue `#4a7cc7` (diagram); serif + sans pairing |

**Logo concept:** mini C4 context diagram (User → Blog System → Email/GitHub, Author → Blog System) crossed out with a bold burnt-orange X; serif "The Not Architect" wordmark; PEOPLE · PROCESS · TECHNOLOGY subheading; italic tagline.

**Brand voice:** direct, opinionated, zero fluff. Business and technology in equal measure. Self-aware humour — the diagram is the start, never the end; delivery is the proof.

## Stack
- **Framework:** Next.js (App Router, Turbopack) — ISR via `revalidate = 60`
- **Content:** Posts stored in Postgres (NeonDB in production, Docker in development). MDX compiled at request time via `next-mdx-remote/rsc`.
- **ORM:** Drizzle ORM (`drizzle-orm` + `postgres` driver); schema in `lib/db/schema.ts`; migrations via `drizzle-kit`
- **Styling:** Tailwind CSS 4 + shadcn-style components; `next-themes` light/dark; GSAP animations
- **Testing:** Vitest + React Testing Library
- **Hosting:** Vercel (deploy via dashboard, not GitHub Actions); DNS/CDN via Cloudflare
- **Package manager:** pnpm

> Exact versions are pinned in `package.json` / `pnpm-lock.yaml` (source of truth) — prefer stable releases ≥10 days old. Run `pnpm audit` after any dependency change.

## Project layout
- `app/` — routes: `page.tsx` (home/post list), `posts/[slug]/` (post page + `opengraph-image.tsx`), `feed.xml/route.ts` (RSS), `sitemap.ts`, `layout.tsx`, `globals.css`
- `app/admin/` — `login/page.tsx`, `new-post/page.tsx` (authenticated post creation UI)
- `app/api/auth/` — `login/route.ts`, `logout/route.ts`
- `app/api/posts/create/route.ts` — inserts new post into DB
- `content/posts/` — legacy MDX files (not served at runtime; used by seed script only)
- `components/` — `ui/` (primitives), `blog/` (PostCard, Nav, Footer, TOC), `animations/`, `providers/`
- `lib/` — `posts.ts` (Drizzle query helpers), `auth.ts` (JWT sign/verify), `utils.ts`
- `lib/db/` — `index.ts` (Drizzle client singleton), `schema.ts` (posts table + types)
- `scripts/seed.ts` — seeds the hello-world post from `content/posts/hello-world.mdx`
- `drizzle/` — migration files generated by drizzle-kit
- `__tests__/` — mirrors src structure
- Config: `drizzle.config.ts`, `docker-compose.yml`, `proxy.ts` (CSP nonce + admin auth guard), `next.config.ts` (security headers + image SVG policy), `postcss.config.mjs`, `tailwind.config.ts`, `vitest.config.ts`

## Authoring posts

### Via the admin UI (preferred)
Go to `/admin/new-post`. Requires `ADMIN_PASSWORD`. Posts publish instantly — no deploy needed. The site revalidates within 60 seconds.

### Via the database directly
Insert into the `posts` table. Required columns: `slug`, `title`, `description`, `date`, `content` (raw MDX body, no frontmatter). Optional: `published` (default `true`), `tags` (text array), `author`, `image`, `toc` (jsonb).

> Full author guide: see **Writing posts** in [`README.md`](./README.md).

## Database

| Environment | Connection |
|---|---|
| Local | Docker Postgres via `pnpm db:up` → `localhost:5432/blog` |
| Production | NeonDB — set `DATABASE_URL` in Vercel env vars |

Schema is defined in `lib/db/schema.ts`. Push changes with `pnpm db:push` (dev) or generate + run migrations with `pnpm db:migrate` (prod).

## Admin auth

Password-based JWT. `proxy.ts` redirects unauthenticated `/admin` requests to `/admin/login`. Cookie expires after 24 hours.

Required environment variables:

| Variable | Purpose |
|---|---|
| `DATABASE_URL` | Postgres connection string |
| `ADMIN_PASSWORD` | Login password for `/admin` |
| `ADMIN_JWT_SECRET` | JWT signing secret (≥32 chars) |
| `NEXT_PUBLIC_SITE_URL` | Production URL (e.g. `https://thenotarchitect.dev`) |

Copy `.env.local.example` to `.env.local` and fill in values for local dev.

## Security Standards (must hold before deploy)
- **HTTP headers** — CSP (strict, nonce-based; set in `proxy.ts`), HSTS (1yr, includeSubDomains, preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/mic/geolocation off). Static headers in `next.config.ts`.
- **Dependencies** — `pnpm audit` clean before every deploy; Renovate enabled; lockfile committed + integrity-checked in CI.
- **Content** — MDX compiled at request time by `next-mdx-remote` (no `eval` of user input); no `dangerouslySetInnerHTML` without sanitisation; `dangerouslyAllowSVG` is enabled only for the first-party logo, sandboxed with `script-src 'none'`.
- **Secrets** — never committed; managed via Vercel env vars. Only `NEXT_PUBLIC_`-prefixed values reach the client (non-sensitive only).

## Testing
≥80% line/branch coverage, enforced in CI (build fails below threshold). Cover: `lib/` helpers + DB schema (unit), components incl. snapshots (RTL), post-page render by slug (integration). Optional E2E (Playwright) for home → post navigation.

CI on every push/PR to `main`: install (`--frozen-lockfile`) → lint → `pnpm audit --audit-level=high` → test with coverage. Build/deploy handled by Vercel.

## Conventions
- Post pages use ISR (`revalidate = 60`), not pure SSG or SSR.
- `generateStaticParams` pre-renders known slugs at build time when a DB is available; falls back to `[]` gracefully so local builds work without a running database.
- Per-page `metadata` export for SEO (title, description, OG, canonical).
- `next/image` for all images — never raw `<img>`.
- No custom webpack config — Turbopack is the default.
- Core Web Vitals targets from day one: LCP < 2.5s, CLS < 0.1, INP < 200ms.

## Remaining milestones
1. [ ] Push to GitHub and connect to Vercel; set env vars in Vercel dashboard
2. [ ] Create NeonDB project; set `DATABASE_URL` in Vercel; run `pnpm db:push` against Neon; run seed
3. [ ] Point custom domain via Cloudflare
4. [ ] Publish first post via `/admin/new-post`

> Done: app scaffold, security headers, post list + post pages, nav/footer/RSS/sitemap, Vitest + coverage gate, GitHub Actions CI, branding + theme toggle + GSAP, NeonDB + Drizzle ORM, admin auth + post creation UI.
